<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="d3.accounting.infrastructure.CreateCatalogTablesMapper">

	<select id="createTemporal" parameterType="java.lang.String">
CREATE TABLE x_${code}_temporal_rmp (
	crmp_llave varchar(32) NOT NULL,
	nrmp_nivel int,
	crmp_cuenta character varying(32) NOT NULL,
	drmp_fechainicio timestamp with time zone NOT NULL,
	drmp_fechafin timestamp with time zone NOT NULL,
	crmp_periodo varchar(100) NOT NULL,
	nrmp_ano int,
	nrmp_mes int,
	nrmp_dia int,
	nrmp_hora int,
	nrmp_minuto int,
	nrmp_cantidad int NOT NULL DEFAULT 0,
	nrmp_promedio NUMERIC(10,2) NOT NULL DEFAULT 0,
	mrmp_saldoanterior NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_saldosiguiente NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_positivo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_negativo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_valor NUMERIC(18,6) NOT NULL DEFAULT 0,
	drmp_creacionfecha timestamptz NOT NULL DEFAULT NOW(),
	crmp_creacionusuario varchar(32),
	crmp_creacionusuarionombre varchar(200),
	drmp_modificacionfecha timestamptz,
	crmp_estado varchar(1) NOT NULL DEFAULT 'A'::character varying,
	CONSTRAINT pk_x_${code}_temporal_rmp PRIMARY KEY (crmp_llave)
);
	</select>
	
	<select id="createPuntual" parameterType="java.lang.String">
CREATE TABLE x_${code}_puntual_rmp (
	crmp_llave varchar(32) NOT NULL,
	nrmp_nivel int,
	crmp_cuenta character varying(32) NOT NULL,
	drmp_fechainicio timestamp with time zone NOT NULL,
	drmp_fechafin timestamp with time zone NOT NULL,
	crmp_periodo varchar(100) NOT NULL,
	nrmp_ano int,
	nrmp_mes int,
	nrmp_dia int,
	nrmp_hora int,
	nrmp_minuto int,
	nrmp_cantidad int NOT NULL DEFAULT 0,
	nrmp_promedio NUMERIC(10,2) NOT NULL DEFAULT 0,
	mrmp_saldoanterior NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_saldosiguiente NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_positivo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_negativo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mrmp_valor NUMERIC(18,6) NOT NULL DEFAULT 0,
	drmp_creacionfecha timestamptz NOT NULL DEFAULT NOW(),
	crmp_creacionusuario varchar(32),
	crmp_creacionusuarionombre varchar(200),
	drmp_modificacionfecha timestamptz,
	crmp_estado varchar(1) NOT NULL DEFAULT 'A'::character varying,
	CONSTRAINT pkx_${code}_puntual_rmp PRIMARY KEY (crmp_llave)
);
	</select>
	
	<select id="createVoucher" parameterType="java.lang.String">
CREATE TABLE x_${code}_comprobante_cmp (
	ccmp_llave varchar(32) NOT NULL,
	ccmp_catalogo varchar(32) NOT NULL,
	ccmp_codigo varchar(100),
	ccmp_tipo varchar(32),
	ccmp_tiponombre varchar(100),
	ccmp_concepto character varying(200) NOT NULL,
	dcmp_fechacomprobante timestamp with time zone NOT NULL,
	mcmp_positivo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mcmp_negativo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mcmp_valor NUMERIC(18,6) NOT NULL DEFAULT 0,
	dcmp_creacionfecha timestamptz NOT NULL DEFAULT NOW(),
	ccmp_creacionusuario varchar(32),
	ccmp_creacionusuarionombre varchar(200),
	dcmp_modificacionfecha timestamptz,
	ccmp_estado varchar(1) NOT NULL DEFAULT 'A'::character varying,
	CONSTRAINT pk_x_comprobante_${code}_cmp PRIMARY KEY (ccmp_llave)
);
	</select>
	
	<select id="createRegister" parameterType="java.lang.String">
CREATE TABLE _x_${code}_registro_reg (
	creg_llave varchar(32) NOT NULL,
	creg_comprobante character varying(32) NOT NULL,
	creg_cuenta character varying(32) NOT NULL,
	creg_descripcion character varying(200),
	creg_tercero character varying(32),
	creg_terceroid character varying(200),
	creg_terceronombre character varying(200),
	creg_centrocosto varchar(32),
	creg_centrocostoid varchar(200),
	creg_centrocostonombre varchar(200),
	dreg_fecha timestamp with time zone NOT NULL,
	mreg_valor NUMERIC(18,6) NOT NULL DEFAULT 0,
	mreg_positivo NUMERIC(18,6) NOT NULL DEFAULT 0,
	mreg_negativo NUMERIC(18,6) NOT NULL DEFAULT 0,
	dreg_creacionfecha timestamptz NOT NULL DEFAULT NOW(),
	creg_creacionusuario varchar(32),
	creg_creacionusuarionombre varchar(200),
	dreg_modificacionfecha timestamptz,
	creg_estado varchar(1) NOT NULL DEFAULT 'A'::character varying,
	CONSTRAINT pkx_registro_${code}_reg PRIMARY KEY (creg_llave)
);
	</select>
	
	<select id="createAuxiliar" parameterType="java.lang.String">
CREATE TABLE x_${code}_auxiliar_aux (
	caux_llave varchar(32) NOT NULL,
	caux_registro character varying(32) NOT NULL,
	caux_dimension character varying(32) NOT NULL,
	caux_valor character varying(200) NOT NULL,
	caux_estado varchar(1) NOT NULL DEFAULT 'A'::character varying,
	daux_creacionfecha timestamptz NOT NULL DEFAULT NOW(),
	caux_creacionusuario varchar(32),
	caux_creacionusuarionombre varchar(200),
	daux_modificacionfecha timestamptz,
	CONSTRAINT pkx_auxiliar_${code}_aux PRIMARY KEY (caux_llave)
);		
	</select>
	
</mapper>