<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.accounting.plan.infrastructure.StackVoucherExtendMapper">

	<select id="stackAvailable" resultType="StackVoucherDTO">
		select cstk_llave as key
			,cstk_comprobante as voucher
			,cstk_estado as state
			,cstk_accion as action
		from account.pila_stk 
		where cstk_estado = 'A' 
		order by dstk_creacionfecha limit 50;
	</select>
	
</mapper>